{
  "name": "placement-prep-backend",
  "private": true,
  "version": "1.0.0",
  "description": "REST API for the Campus Placement Accelerator",
  "type": "commonjs",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env --watch server.js",
    "db:migrate": "node --env-file=.env scripts/migrate.js",
    "db:migrate-json": "node --env-file=.env scripts/migrate-json.js",
    "db:seed": "node --env-file=.env scripts/seed.js"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "engines": {
    "node": ">=20"
  }
}